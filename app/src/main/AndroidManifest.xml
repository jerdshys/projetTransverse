<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jerome.src" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Server.ServerActivity"
            android:label="@string/title_activity_list_acttivity" >
        </activity>
        <activity
            android:name=".ServerDetail.EditServerActivity"
            android:label="@string/title_activity_edit_item" >
        </activity>
        <activity
            android:name=".NewServer.NewServerActivity"
            android:label="@string/title_activity_new_server" >
        </activity>
    </application>

</manifest>
